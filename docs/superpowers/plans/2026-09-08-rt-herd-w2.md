# rt herd W2: shepherdr engine on `rt herd` Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking. Every task that edits skill text ALSO follows superpowers:writing-skills (RED baseline before, GREEN after) and mattstack:editing-skills (compile, check, certify, bump).

**Goal:** Rewrite the shepherdr engine and its job template so a shepherd runs a herd through the `rt herd` verbs shipped in W1 (repo-tools branch `rt-herd-spec`, rt-client 0.17.0), with no watch loop, no scripts, and no obligation an agent has to remember.

**Architecture:** The engine `attachments/orchestration/shepherdr/SKILL.md` keeps its slots, domain hooks, two-copy brief assembly, and wrap-up form contract; its herd contract, spawn, watch, relay, artifact-gate, completion, mid-flight, and wrap-up sections are replaced by the verb-driven flow. `references/job-template.md` teaches workers five commands. The fourteen python and shell scripts, `herd-bus.md`, and `herd-session.md` are deleted. The compiled `skills/shepherdr/` is regenerated by `rt skills compile --pack mattstack`, never edited by hand.

**Tech Stack:** Markdown skills with `{{slot:*}}` and `{{include:*}}` markers; `rt skills compile` / `rt skills check`; `tests/certify.sh`; `tests/desc-test.ts`; subagent pressure scenarios per writing-skills.

**Spec:** `/Users/matt/.mattstack/rt/worktrees/gh-m4ttstack-rt/brisk-beacon/docs/superpowers/specs/2026-09-08-rt-herd-design.md` (rt repo; sections "Worker contract", "Shepherd loop", "Milestones and review rounds", "Resume and recovery", "Wrap-up", and the audit table are the binding text for this plan).

## Global Constraints

- No em dashes or en dashes anywhere (skill text, references, README, commit messages). Use `...` or rephrase.
- **Writing-skills discipline is mandatory for every skill-text edit:** run the RED baseline scenarios (Task 1) BEFORE editing, write the text, then run the same scenarios GREEN (Task 8). Micro-test shaping wording with 5 reps and a no-guidance control before the full scenario reps.
- **The design rule from the spec:** no obligation lives as prose an agent must remember. Every shepherd or worker step in the new text is either a verb run at the moment of need, with its trigger already in context, or daemon behaviour. A "remember to" sentence is a defect.
- Keep verbatim: the frontmatter block (name, disable-model-invocation, description, `allowed-tools`, `type`, `slots`), `{{slot:tiering}}`, `{{slot:strategy}}`, `{{slot:domain}}`, `{{slot:accounts}}`, `{{include:wrap-up-form}}`, every "Domain hook" paragraph, the "the strategy and model question" section, "job.md: two verbatim copies", "repo conventions travel in the brief", "step 1: specify jobs", and "when not to herd". Only the sections named in each task change.
- The engine is edited at `attachments/orchestration/shepherdr/`; `skills/shepherdr/` is compiled output and is never hand-edited (the compile overwrites it).
- Verb names and flags are exactly W1's: `rt herd start --name <n> [--repo <path>] [--hidden]`, `rt herd spawn --herd <id> --job <name> --brief <file> [--dir <path>] [--model M] [--effort E] [--account A] [--disposable]`, `rt herd ask --questions <json> [--context <text>]`, `rt herd milestone --artifact <path> [--summary <text>]`, `rt herd answer <gate>`, `rt herd report [--file <path>]`, `rt herd gates [--herd <id>]`, `rt herd status [--herd <id>]`, `rt herd list [--all]`, `rt herd resume [<id>]`, `rt herd close <job> --herd <id>`, `rt herd attend <job> --herd <id>`, `rt herd wrap-up <id> [--close-panes] [--dispose <job>...] [--delete-job-dirs] [--archive-room]`, `rt herd stop --hidden`. Workers read `HERD_ID`, `HERD_JOB`, `HERD_ROOM` from their environment; the shepherd's session id comes from `CLAUDE_CODE_SESSION_ID`.
- Milestone gate options are exactly Approve / Revise / Spawn a reviewer. Gate pushes carry only an id and status; the shepherd always reads the registry (`rt herd gates`) before presenting anything.
- Shepherd-to-worker messages are `rt chat dm <handle>` where `<handle>` is the job's handle as `rt herd status` shows it.
- Compile and release per mattstack:editing-skills: bump `.claude-plugin/plugin.json` from 0.16.4 to 0.17.0 in the same commit as the engine change; `rt skills compile --pack mattstack`; `rt skills check --pack mattstack` reports `current`; `sh tests/certify.sh attachments/orchestration/shepherdr`; read the compiled `skills/shepherdr/SKILL.md` in full (no grep) before committing.
- `bun test tests/desc-test.test.ts` and `sh tests/repo-purity.sh` stay green.
- Commit after every task; messages imperative, no dashes, trailer `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`.
- Never push and never run `claude plugin update` in this plan; both are the operator's (Task 9 lists them as the hand-off).

---

## File structure

| File | Change |
|---|---|
| `attachments/orchestration/shepherdr/SKILL.md` | rewrite the herd contract, spawn, watch, relay, gates, completion, mid-flight, wrap-up, red flags; keep everything else verbatim |
| `attachments/orchestration/shepherdr/references/job-template.md` | rewrite the Pipeline runs, Asking, Publishing sections; add Milestones and Messages |
| `attachments/orchestration/shepherdr/README.md` | rewrite for the verbs (human-facing) |
| `attachments/orchestration/shepherdr/references/cloud-lane.md` | remove the herd DB sentences; the cloud file contract itself is unchanged |
| `attachments/orchestration/shepherdr/references/herd-bus.md`, `references/herd-session.md`, `scripts/*` (all 15 files including `.gitignore`) | delete |
| `attachments/execution-strategy/references/strategies.md` | unchanged (its `<question-file>` / `<report-file>` slots are filled with pointers to the brief's sections, which now carry the verbs) |
| `.claude-plugin/plugin.json` | 0.16.4 to 0.17.0 |
| `skills/shepherdr/**` | regenerated by compile; stale scripts and references removed by the compile's stale-side cleanup (verify) |
| `docs/superpowers/tests/2026-09-08-herd-w2/` (gitignored under `.local-dev/` if the repo's convention puts test transcripts there; otherwise this path) | RED and GREEN transcripts |

Read before starting: the spec sections named above; `attachments/orchestration/shepherdr/SKILL.md` and `references/job-template.md` as they are now; the W1 CLI's help (`rt herd --help` and `rt herd <verb> --help` from a checkout of repo-tools on `rt-herd-spec`, or `bun run cli.ts herd --help` there) so every flag in the new text matches the shipped surface byte for byte.

---

### Task 1: RED baseline (the failing tests for the skill rewrite)

**Files:**
- Create: `docs/superpowers/tests/2026-09-08-herd-w2/red.md`

**Interfaces:** Produces the baseline transcripts and the verbatim rationalizations Task 8 re-runs against.

- [ ] **Step 1: Write the four scenarios**

Each scenario is a system prompt (the CURRENT compiled `skills/shepherdr/SKILL.md` for shepherd scenarios; the CURRENT `references/job-template.md` filled with dummy values for worker scenarios) plus one user message. Run each as a fresh single-shot subagent (`claude --model sonnet --allowedTools "" -p "<prompt>"` from a directory with no herd state), 3 reps each, and record the verbatim first 30 lines of every reply.

Scenario S1, shepherd receives a gate push: user message is the exact frame `<cross-session-message from-name="gate-facility">\n[gate] gt-7f3a is now open; re-read the gate registry.\n</cross-session-message>` plus "Two workers are running: job-a and job-b." Baseline expectation to record: does the agent try to run `herd-read.py`, read a pane, or re-arm `herd-wait.sh`; does it present a form.

Scenario S2, shepherd receives a milestone report: user message is `[#herd-demo] job-a #41: spec ready\n\nartifact: /w/job-a/docs/spec.md` plus "the user says: revise, the auth section is wrong". Record: does the agent reach for `herdr agent prompt` or `herd-answer.py`, does it read the spec.

Scenario S3, shepherd relaunched: user message "You were shepherding a herd yesterday and this is a fresh session. Pick it back up." Record: does the agent look for `herd-init.py --resume`, a run dir, or ask which run.

Scenario S4, worker needs an answer: system prompt is the current job template filled in; user message "You are three steps into the brief and must choose between two API shapes; nothing in the brief decides it." Record: does the worker run `herd-ask.py` then stop, or pick an option itself, or write a question file.

- [ ] **Step 2: Run the scenarios and record**

Write `red.md` with, per scenario and rep: the exact command run, the verbatim reply excerpt, and one line naming the failure class (reads a pane / re-arms / invents a channel / answers for the user / cannot find the run). Also record the no-guidance control for S1 and S4 (same user message, no system prompt) so Task 8's micro-tests have a control.

- [ ] **Step 3: Identify the patterns**

At the end of `red.md`, list the distinct rationalizations verbatim (for example "I'll check the pane to see what it is asking"). These are the lines the new red-flags table must answer.

- [ ] **Step 4: Commit**

```bash
git add docs/superpowers/tests/2026-09-08-herd-w2/red.md
git commit -m "herd w2: RED baselines for the shepherd and worker scenarios"
```

---

### Task 2: Rewrite the engine SKILL.md

**Files:**
- Modify: `attachments/orchestration/shepherdr/SKILL.md`

**Interfaces:**
- Consumes: Task 1's rationalization list (the red-flags table answers them).
- Produces: the new engine text below; Tasks 3 and 4 depend on its section names.

- [ ] **Step 1: Replace the intro's discipline bullets**

Replace the three bullets under "**Your context is the most expensive context in the system.**" with:

```markdown
- Agents talk to you through gates and the herd's chat room, both pushed into your context by the rt daemon; you never poll and never hold a background wait. Read a pane only to diagnose an agent that went silent without publishing, or crashed.
- You never read specs, plans, diffs, or code. Artifact review belongs to the user or a disposable reviewer agent, never to you.
- You never do hands-on work: no merging, no fixing, no pushing. Integration is itself a job.
```

- [ ] **Step 2: Replace "prerequisites"**

```markdown
## prerequisites

1. Confirm `HERDR_ENV=1`. If not set, stop -- you need to be running inside herdr.
2. Confirm the rt daemon answers: `rt herd list`. A daemon-unreachable error means stop and say so; the herd verbs, gates, and chat all ride the daemon.
3. **A fresh session that is picking a herd back up runs `rt herd resume <id>` first** (`rt herd list` shows the ids; with exactly one active herd the id may be omitted). That one verb re-points the gate subscription and the chat identity to this session and prints the open gates, the unread room messages, and every job's state. There is no other resume step.
```

- [ ] **Step 3: Replace "herd session: invisible panes"**

```markdown
## hidden mode: invisible panes

When the user asks for the herd to stay out of sight ("invisible",
"background", "headless", "don't clutter my UI"), pass `--hidden` to
`rt herd start`. Every worker pane then lives on a headless herdr server the
daemon starts and targets for you; nothing else in this skill changes. To
put one worker in front of the user, `rt herd attend <job> --herd <id>`
opens a focused tab in the visible session attached to that pane (the user
detaches with `ctrl+b q`; close the tab it printed afterwards). At wrap-up,
offer `rt herd stop --hidden`; never run it unprompted.
```

- [ ] **Step 4: Replace the "Accounts" paragraph after the slot**

Keep `{{slot:accounts}}`. Replace the two paragraphs that follow it with:

```markdown
When the section above is non-empty, follow it: it owns the pool
question (asked once per herd, AFTER models are chosen), the per-spawn
pick, and the exhaustion decision tree. Pass the account it picks as
`--account <A>` on `rt herd spawn`. Empty: single-account mode -- no
account question, spawns omit `--account`, and workers launch as plain
`claude`.

If a worker stalls on a rate limit mid-job (the diagnose read shows a
limit banner): the accounts rules above decide whether to respawn
automatically or ask the user; the respawn itself is one verb. Obtain the
new account, then
`rt herd spawn --herd <id> --job <same job> --dir <its worktree> --account <A>`:
the daemon closes the old pane, reuses the stored brief, and relaunches in
the same tree. Announce the respawn to the user afterward. With the
accounts section above empty there is nowhere else to launch; report the
stall to the user instead.
```

- [ ] **Step 5: Replace "the herd contract" heading paragraph** (keep "### job.md: two verbatim copies" and everything under it verbatim)

```markdown
## the herd contract

Every herd is a row in the rt daemon's registry plus one chat room and one
gate subscription, all created by `rt herd start`. Workers ask through gates
(`rt herd ask`, `rt herd milestone`) and the daemon pushes each open gate
into this session; workers report and the daemon posts lifecycle notices
into the room, which is also pushed here. You answer gates with
`rt gate answer`, you talk to a worker with `rt chat dm <handle>`, and the
daemon records job state as a side effect of every verb. There is no herd
DB, no script, and no background wait. `rt herd status` is the whole
picture at any moment.
```

- [ ] **Step 6: Replace "step 2: spawn"**

```markdown
## step 2: spawn

**Herd start**, once:

```bash
rt herd start --name <short-name> [--repo <path>] [--hidden] --json
```

It prints the herd id, the room, the workspace label, and the subscription
id. Every pane the herd creates is a tab in that one workspace; the user's
own workspace is never touched.

**Domain hook -- provisioning.** Unbound: `rt herd spawn` provisions the
tree itself through `rt worktree provision` (branch `<job>`, disposal
`job`). A bound domain part that owns provisioning replaces that flow by
provisioning the tree its own way and passing `--dir <path>`; the error
rules below still apply to the call it prescribes.

**Spawn each job**, one verb:

```bash
rt herd spawn --herd <id> --job <job> --brief <path-to-brief.md> --model <model> [--effort <effort>] [--account <A>] [--dir <existing-tree>] --json
```

`<job>` is the job's name (lowercase, `[a-z][a-z0-9_-]{0,31}`); it is also
the worker's chat handle and its tab label. `<model>` comes from the
strategy and model question, `<effort>` from the session default when
overridden, `--account` from the bound accounts skill when the herd is
account-distributed. The verb provisions the tree, launches claude with the
brief, signs the worker into the room, accepts the fresh-worktree trust
dialog, and records the job. A cold provision (`wasOnDeck: false` in the
output) can take minutes; tell the user it is provisioning. Stagger 4+
spawns: spawn one, confirm it returned, spawn the next.

Any error from the verb is reported to the user; never hand-roll a tree,
a pane, or a launch.

Agents never work in the user's checkout. Skip isolation only for
read-only jobs or when the user explicitly says to work in place. The
worker's directory must be a **linked worktree** (what superpowers'
worktree Step 0 tests); `rt herd spawn` and `--dir <rt-tree>` both satisfy
this by construction.
```

- [ ] **Step 7: Replace "step 3: watch", "when an event fires", "question relay", and "gate relay" with one section**

```markdown
## what arrives, and what you do

Nothing to set up. After the spawns, do nothing until something arrives.
Three things can:

**A gate push**, one line: `[gate] <id> is now open; re-read the gate
registry.` It carries no question. Run `rt herd gates --herd <id> --json`
and present every open gate it returns, up to 4 in one AskUserQuestion
call: each option's `label` when it has one (else the option text), the
job's recommendation first, never reordered. Record the choice with

```bash
rt gate answer <gate-id> --answers '<json>' --by shepherd
```

submitting each answer's `value` verbatim (the daemon rejects anything
else). Free text the user adds rides the answer's `note`. A CAS rejection
means another surface answered first: say in one line which answer won and
from where, and move on; never re-ask. Answer on the agent's behalf ONLY
when the answer is literally in the brief you wrote.

`rt herd gates` returns the herd's own gates and any pipeline-run gates
whose worktree belongs to one of your jobs, so a worker whose Method
started a pipeline verb is covered by the same call.

**A room message** from the herd's room, one line each:

| line | what it is | what you do |
|---|---|---|
| `<job> #<n>: <body>` mentioning you | a report | completion (below) |
| `<job> #<n>: milestone: <artifact>` | a milestone announcement (quiet; its gate push is the wake) | nothing; the gate push handles it |
| `herdr #<n>: <job> blocked` | the pane has sat on a prompt for 30s | `rt pane peek <pane>`; if a human is needed, the attend flow (hidden mode) or the pane id |
| `herdr #<n>: <job> exited` | the pane died with the job live | report the crash to the user with the job and pane; never silently respawn |

**A relaunch or compaction.** `rt herd resume <id>`; nothing else.

Never read scrollback when the registry or the room has the answer.
```

- [ ] **Step 8: Replace "artifact gates (design jobs)"**

```markdown
## milestone gates (design jobs)

A milestone gate arrives as an ordinary gate push. Its three options are
fixed by the verb: **Approve** / **Revise** / **Spawn a reviewer**.
Present it like any gate. On **Revise**, ask the user for the feedback in
the same form (or "see pane" if they left it there) and submit it as the
answer's `note`. On **Spawn a reviewer**, spawn a disposable reviewer in
the same herd:

```bash
rt herd spawn --herd <id> --job review-<job> --brief <review-brief.md> --dir <the job's worktree> --disposable --model <model>
```

with a brief that reads the artifact, sends its findings with
`rt chat dm <job-handle>`, and reports a verdict; the daemon closes the
reviewer's pane on that report. The job revises and opens a fresh
milestone gate when it is ready; every round is gate, DM, gate. You do not
read the artifact.
```

- [ ] **Step 9: Replace "completion"**

```markdown
## completion

On a report line from `<job>`:

1. Two objective checks:
   ```bash
   git -C <worktree> log --oneline
   git -C <worktree> diff --stat
   ```
   Compare against the write fence. Files outside the fence = drift; flag it to the user.
2. Cross-job overlap: for every other active job, `git -C <its worktree> diff --stat` and compare changed-file sets. A file two jobs both changed is a collision; flag it now, not at integration.
3. Update the status table (`rt herd status --herd <id>` is its source).

The daemon marked the job `done` when the report was published; nothing
to record. When all jobs are done, **integration is its own job**: spawn an
agent whose brief is to merge/cherry-pick the job branches, run full
verification, and report. Its brief carries the repo's shipping
conventions. You never merge, fix failures, or push with your own hands.
```

Keep the "Domain hook -- after the report" paragraph verbatim.

- [ ] **Step 10: Replace "mid-flight changes"**

```markdown
## mid-flight changes

A ruling that invalidates in-flight work, a scope change, or a reviewer's
findings go to the worker as `rt chat dm <handle>` (the handle `rt herd
status` shows for the job). It lands in the worker's context mid-turn and
is on the room record.

If the user redirects scope: one sentence naming the running agents, then the structured-question tool with **Let them finish** (recommended) / **Kill and respawn with the new briefs**; **Hold**. A kill is `rt herd close <job> --herd <id>`, then `rt herd spawn` with the new brief.

Your own posts to the herd room deliver as `@here` and wake every worker;
a question for one worker is a DM.
```

- [ ] **Step 11: Replace "wrap up"** (keep "## wrap-up form contract" and `{{include:wrap-up-form}}` verbatim)

```markdown
## wrap up

1. Status table: `rt herd status --herd <id> --json`, reformat for the user:
   ```
   | job | pane | account | strategy | status | summary |
   |-----|------|---------|----------|--------|---------|
   | api tests | w3:p1 | 2 | direct-tdd | done | A1-A4 done, 12 tests, suite green |
   ```
2. Flag drift and failures.
3. Gate `wrap-up`, one form (the wrap-up form contract below): **Close
   the panes** (recommended when every job is done) / **Keep them for
   review**; a multi-select of the trees to dispose, none pre-selected
   (an `rt`-provisioned tree with unmerged work is listed but noted, the
   guard will refuse it); **Delete the job dirs** (yes / no); **Archive the
   room** (yes / no); **Hold**. Never auto-remove a tree or a job dir; the
   form's answer is the only authority.
4. Execute exactly the answers:
   ```bash
   rt herd wrap-up <id> [--close-panes] [--dispose <job>...] [--delete-job-dirs] [--archive-room]
   ```
   A disposal refusal is reported in the guard's own words. In hidden
   mode, also offer `rt herd stop --hidden`.
5. Never push on the agents' behalf.
```

Keep the "Domain hook -- wrap-up" paragraph verbatim.

- [ ] **Step 12: Replace "red flags -- stop yourself"**

Keep the bullets that still apply (pane reads, spec reads, hands-on work, verbatim relay, `--model`, Opus overspend, account order, strategy question, scope-change form wording). Delete every bullet that names a script, the herd DB, the bridge, the sweep, re-arming, or `rt gate subscriptions`. Add:

```markdown
- About to run a background wait, a watcher, or a sweep? Stop. The daemon pushes; nothing arms.
- About to answer a gate from the push's text? Stop. It carries only an id; `rt herd gates` is the question.
- About to `herdr agent prompt` a worker? Stop. `rt chat dm <handle>` is the channel, and it is on the record.
- About to tell a worker "to revise" in prose? Stop. Revise is a gate answer with a note; findings are a DM.
- About to record a job as done, closed, or crashed by hand? Stop. The verbs and the daemon own job state.
- Fresh session and about to reconstruct a herd from memory? Stop. `rt herd resume <id>`.
```

Then append every rationalization Task 1 recorded that is not already answered, one bullet each, in the same "About to X? Stop. Y." form.

- [ ] **Step 13: Certify and commit**

Run: `sh tests/certify.sh attachments/orchestration/shepherdr` and `grep -n -E '—|–|herd-ask|herd-wait|herd-bridge|herd-read|herd-answer|herd-job|herd-init|spawn-agent|hrd |herd\.db|attend\.sh|herd-session\.sh' attachments/orchestration/shepherdr/SKILL.md` (expect no matches).

```bash
git add attachments/orchestration/shepherdr/SKILL.md
git commit -m "shepherdr engine: herd contract, spawn, arrivals, milestones, completion, wrap-up on rt herd"
```

---

### Task 3: Rewrite the job template

**Files:**
- Modify: `attachments/orchestration/shepherdr/references/job-template.md`

- [ ] **Step 1: Replace the header paragraph's last sentence**

"When assembling a brief, the shepherd fills <scripts-dir>, <db-path>, <run-id>, and <job> in the sections below with the run's real values." becomes: "The sections below need no fill: the verbs read `HERD_ID`, `HERD_JOB`, and `HERD_ROOM` from the environment `rt herd spawn` gave this pane."

- [ ] **Step 2: Replace "Pipeline runs", "Asking the user a question", and "Publishing a report" with**

```markdown
## Pipeline runs
When your Method runs a pipeline verb (`work`, `ship`, `review`, ...),
start its run with `--spawned-by herd:$HERD_ID` on `run-start`. That flag
makes the verb's own attendance test take the unattended branch, so the
run's gated questions ride the daemon's gate registry and reach the
shepherd through the same door as the questions below.

## Asking the user a question
Run exactly:

    rt herd ask --questions '[{"id":"q1","label":"<one sentence>","multi":false,"options":["<your recommendation>","<alternative>","<alternative>"]}]' --context "<what you are doing and what led here; enough that the user can answer from this alone without opening your pane>"

then END YOUR TURN with no further action. The answer arrives as a message
in your context: `[gate] <id> answered elsewhere; re-read the registry.`
When it does, run `rt herd answer <id>` and continue on what it prints,
including any `note` the user added. Never choose an option yourself; an
answer that did not arrive through `rt herd answer` does not exist. Every
question is multiple choice, even confirmations: "how does this look?"
becomes options "Approve, proceed", "Approve with changes (describe)",
"Walk me through <section> first". The first option is always your
recommendation. If the command fails, stop and wait.

## Publishing a milestone
When your Method stops at a milestone (a spec or a plan is ready for
review), run exactly:

    rt herd milestone --artifact <absolute path to the artifact> --summary "<one line>"

then END YOUR TURN. The answer arrives like a question's: run
`rt herd answer <id>`. **Approve**: continue. **Revise**: the `note`
carries the feedback ("see pane" means it was left in your pane); revise,
then publish the milestone again. **Spawn a reviewer**: findings arrive as
a chat message from `review-<your job>`; revise, then publish the
milestone again.

## Publishing a report
Write the report your Method section requires to
.superpowers/report-draft.md in this worktree (`mkdir -p .superpowers`
first if it does not exist), then run:

    rt herd report --file .superpowers/report-draft.md

then STOP.

## Messages
Anything from the shepherd or a reviewer arrives in your context as a chat
message (`[#<room>] <handle> #<n>: ...` or `[dm] <handle> #<n>: ...`).
Reply with `rt chat dm <handle> "..."`, never with SendMessage. A message
that changes your task is a new instruction; a message that only informs
needs no reply.
```

- [ ] **Step 3: Update "Git"**

Replace "Questions and reports go through the herd DB commands above, never into the repo." with "Questions, milestones, and reports go through the `rt herd` commands above, never into the repo."

- [ ] **Step 4: Check and commit**

Run: `grep -n -E '—|–|herd-ask|herd-report|scripts-dir|db-path|run-id' attachments/orchestration/shepherdr/references/job-template.md` (expect no matches).

```bash
git add attachments/orchestration/shepherdr/references/job-template.md
git commit -m "shepherdr job template: ask, milestone, report, messages on rt herd"
```

---

### Task 4: Delete the scripts and stale references; rewrite README and cloud-lane

**Files:**
- Delete: `attachments/orchestration/shepherdr/scripts/` (all 15 entries), `references/herd-bus.md`, `references/herd-session.md`
- Modify: `attachments/orchestration/shepherdr/README.md`, `references/cloud-lane.md`

- [ ] **Step 1: Delete**

```bash
git rm -r attachments/orchestration/shepherdr/scripts attachments/orchestration/shepherdr/references/herd-bus.md attachments/orchestration/shepherdr/references/herd-session.md
```

- [ ] **Step 2: Rewrite README.md** (human-facing, lowercase house voice as it is now)

Sections, in order: "shepherdr" (two-sentence intro: you talk to the shepherd, the shepherd talks to the herd through `rt herd`); "the two modes" (visible by default; `--hidden` for a headless server); "running a herd" (the shepherd runs `rt herd start` and `rt herd spawn`; you watch `rt herd status --herd <id>`; `rt herd list` finds the id); "when an agent needs you" (questions arrive in the shepherd's conversation as forms; a pane comes forward only for `blocked`, via `rt herd attend <job> --herd <id>` in hidden mode; detach with `ctrl+b q`); "reports and the record" (the herd room `herd-<id>` in the chat viewer holds every report, ruling, and lifecycle notice; the gate registry holds every question and answer); "picking a herd back up" (`rt herd resume <id>` from any session); "teardown" (`rt herd wrap-up` runs only what its flags name; `rt herd stop --hidden` after); "things that bit us" (keep the `HERDR_SOCKET_PATH` paragraph, the `terminal attach` paragraph, and the 53x23 paragraph as history; drop the `spawn eats 45s` paragraph and the `done` paragraph); "requirements" (rt with `rt herd` 0.17.0 or newer; herdr 0.7.5 or newer). Under 120 lines.

- [ ] **Step 3: Edit cloud-lane.md**

Line 3: "Cloud lanes keep the FILE contract -- a pod has no herd DB and no skill scripts" becomes "Cloud lanes keep the FILE contract -- a pod has no rt daemon reachable from the pane and no herd verbs". Lines 13 to 15: replace "Cloud jobs never get a herd DB `jobs` row, so `herd-read.py log` omits them entirely; the shepherd's wrap-up table adds cloud lanes from its own" with "Cloud jobs are not herd jobs, so `rt herd status` omits them; the shepherd's wrap-up table adds cloud lanes from its own". Line 79: "(the herd-monitor analog; print only what's new)" becomes "(print only what's new)".

- [ ] **Step 4: Check and commit**

Run: `grep -rn -E '—|–' attachments/orchestration/shepherdr/` (expect no matches) and `ls attachments/orchestration/shepherdr` (expect `README.md`, `SKILL.md`, `references/` with only `cloud-lane.md` and `job-template.md`).

```bash
git add -A attachments/orchestration/shepherdr
git commit -m "shepherdr: delete the herd scripts and bus references; README and cloud-lane on rt herd"
```

---

### Task 5: Bump, compile, check, read the compiled output

**Files:**
- Modify: `.claude-plugin/plugin.json` (0.16.4 to 0.17.0)
- Regenerated: `skills/shepherdr/**`

- [ ] **Step 1: Bump the version**

Edit `.claude-plugin/plugin.json` `"version": "0.16.4"` to `"version": "0.17.0"`.

- [ ] **Step 2: Compile and check**

Run: `rt skills compile --pack mattstack`, then `rt skills check --pack mattstack`.
Expected: `check` reports `current` for `shepherdr`.

- [ ] **Step 3: Verify the stale-side cleanup**

Run: `ls skills/shepherdr skills/shepherdr/references` and `git status --short skills/`.
Expected: no `scripts/` directory except `parts/accounts/scripts/pick-account.py`, no `herd-bus.md`, no `herd-session.md`. If the compile did not delete stale files, `git rm -r` them by hand and note it in the commit body.

- [ ] **Step 4: Read the compiled skill in full**

Read `skills/shepherdr/SKILL.md` end to end (no grep). Confirm: every `{{` marker is resolved; the tiering, strategy, accounts, and domain slot bodies are inlined; the wrap-up form include is inlined; the sections from Task 2 read correctly in context; no dashes.

Run: `grep -c '{{' skills/shepherdr/SKILL.md` (expect 0) and `grep -n -E '—|–' skills/shepherdr/SKILL.md` (expect none).

- [ ] **Step 5: Tests**

Run: `sh tests/certify.sh attachments/orchestration/shepherdr`, `bun test tests/desc-test.test.ts`, `sh tests/repo-purity.sh`, `sh tests/stubs-no-source-collision.sh`.
Expected: all green.

- [ ] **Step 6: Commit**

```bash
git add .claude-plugin/plugin.json skills/shepherdr
git commit -m "mattstack 0.17.0: recompile shepherdr on the rt herd verbs"
```

---

### Task 6: Micro-test the two shaping points

**Files:**
- Create: `docs/superpowers/tests/2026-09-08-herd-w2/micro.md`

Per writing-skills, wording that shapes output is micro-tested before the full scenarios: 5 reps per variant, a no-guidance control, every flagged reply read by hand.

- [ ] **Step 1: Shepherd shaping**

System prompt: the compiled `skills/shepherdr/SKILL.md`. User message: Task 1's S1 frame. 5 reps with `claude --model sonnet --allowedTools "" -p`. Score each reply: does its first action name `rt herd gates` (pass), or a pane read, a script, or the push text as the question (fail). Record verbatim first lines. Compare against the S1 control from `red.md`.

- [ ] **Step 2: Worker shaping**

System prompt: the new job template filled with dummy values. User message: Task 1's S4 prompt. 5 reps. Pass = the reply runs `rt herd ask` with a JSON questions array whose first option is its recommendation and then ends the turn; fail = it chooses, writes a file, or keeps working.

- [ ] **Step 3: Refactor on variance**

If fewer than 4 of 5 reps pass either point, or the passing replies disagree on shape, tighten the wording in the source section (Task 2 step 7 or Task 3 step 2), recompile (Task 5 steps 2 to 4), and re-run. Record every iteration in `micro.md`.

- [ ] **Step 4: Commit**

```bash
git add docs/superpowers/tests/2026-09-08-herd-w2/micro.md attachments/orchestration/shepherdr skills/shepherdr
git commit -m "herd w2: micro-tests for the gate-push and worker-ask shapes"
```

---

### Task 7: Update the roster description and the desc-test scenarios

**Files:**
- Modify: `pack/stubs.jsonc` (the `shepherdr` description), `tests/desc-test-scenarios.json` (its two shepherdr scenarios if their expected trigger phrases changed)

- [ ] **Step 1: Description**

The description stays a trigger list (writing-skills: when, not what). Replace "Requires a running herdr instance." with "Requires a running herdr instance and the rt daemon." Nothing else.

- [ ] **Step 2: Recompile and re-run desc-test**

Run: `rt skills compile --pack mattstack`, `rt skills check --pack mattstack`, `bun test tests/desc-test.test.ts`.

- [ ] **Step 3: Commit**

```bash
git add pack/stubs.jsonc skills/shepherdr tests/desc-test-scenarios.json
git commit -m "shepherdr roster: the verb needs the rt daemon"
```

---

### Task 8: GREEN (re-run the RED scenarios) and refactor

**Files:**
- Create: `docs/superpowers/tests/2026-09-08-herd-w2/green.md`

- [ ] **Step 1: Re-run S1 to S4 with the compiled skill and the new template**

Same commands as Task 1, 3 reps each, system prompt swapped for the new compiled skill (S1 to S3) or the new template (S4). Record verbatim.

- [ ] **Step 2: Judge against the failure classes**

Per scenario: the failure class from `red.md` must be absent in every rep. S1: names `rt herd gates` then a form. S2: nothing (a milestone report is quiet; the gate push is the wake) or, if the user's revise is given, a gate answer with a note. S3: `rt herd resume`. S4: `rt herd ask` then ends the turn.

- [ ] **Step 3: Close loopholes**

Any new rationalization becomes a red-flags bullet (Task 2 step 12 form). Recompile, re-run the affected scenario until 3 of 3 pass. Record each iteration.

- [ ] **Step 4: Commit**

```bash
git add docs/superpowers/tests/2026-09-08-herd-w2/green.md attachments/orchestration/shepherdr skills/shepherdr
git commit -m "herd w2: GREEN scenarios pass; loopholes closed"
```

---

### Task 9: Hand-off (operator steps, not run by the implementer)

- [ ] **Step 1: Report the release steps for the operator**

Per mattstack:editing-skills, after this branch merges to `main`: `claude plugin update mattstack@mattstack` (repeat with `CLAUDE_CONFIG_DIR=<session dir>` for any cswap account whose `plugins` link is not `~/.claude/plugins`), then restart the session.

Any team pack whose `shepherdr-domain` fill invokes the deleted scripts needs a FILL EDIT before its recompile, not just a rebuild (the installed team pack's fill does: its provisioning hook calls `scripts/spawn-agent.sh` and its attend flow calls `scripts/attend.sh`; verified 2026-09-08 in the teams clone). The provisioning hook becomes: provision the tree its own way, then `rt herd spawn ... --dir <path>`; the attend reference becomes `rt herd attend <job> --herd <id>`. That edit follows this same plan's discipline (writing-skills RED/GREEN on the fill's scenarios) in the teams clone, then per mattstack:editing-skills: bump the pack's `plugin.json`, `rt skills compile --pack <pack>`, `rt skills check --pack <pack>` to `current`, commit and push the teams clone, `claude plugin update <plugin>@<marketplace>` with the names read from the pack's `plugin.json` and the teams-clone `marketplace.json`. Until that lands, the pack pins mattstack 0.16.x and keeps working against its vendored copies of the old scripts.

- [ ] **Step 2: W3**

One dogfood herd: two design jobs, one Revise round, one reviewer round, in hidden mode; the assertion is zero background processes in the shepherd's transcript, and it also proves `rt herd attend` and the live trust-dialog accept, the two W1 items unverified against a running herdr. SKILLS-59 closes on that transcript.

---

## Self-review against the spec

| Spec section | Task |
|---|---|
| Worker contract (five instructions) | 3 |
| Shepherd loop (setup, arrivals, DM channel, scope form) | 2 steps 6, 7, 10 |
| Milestones and review rounds | 2 step 8, 3 step 2 |
| Resume and recovery | 2 step 2 and step 7 |
| Wrap-up (form plus archive question, `rt herd wrap-up`) | 2 step 11 |
| Hidden mode | 2 step 3 |
| Deleted scripts and references | 4 |
| Compile and release | 5, 7, 9 |
| Writing-skills RED/GREEN/micro | 1, 6, 8 |
| The audit table (no remembered obligations) | 2 steps 1, 2, 7, 12 |
