# Skill certification

The written purity rule and per-skill certification checklist for this repo
(spec: the thinning milestone / certification sweep). The machine gate is
`tests/certify.sh <skill-dir>`; this file is the human half plus the ledger.

## Purity rule

1. Nothing domain-specific to any one team ships in this repo. Domain
   conventions, examples, and routing live in domain-owned packs that BIND
   to the seams here (slots, pipelines, manifests).
2. Nothing personal to one operator ships in prose or scripts: no personal
   names, home paths, or account hints. The bar is the README's: what
   someone else could actually pick up.
3. House style: no em or en dashes; trigger-only descriptions
   ("Use when...", <= 500 chars) per superpowers:writing-skills.

## Certification checklist (per skill)

- [ ] `tests/certify.sh <skill-dir>` exits 0 (purity greps, no-ticket-id
      ban, frontmatter grammar, depth cap, vendored-resolver identity,
      stage declaration).
- [ ] desc-test selection micro-test: all scenarios in
      `tests/desc-test-scenarios.json` pass before AND after any
      description edit (`bun run tests/desc-test.ts`).
- [ ] Parameterized wrappers only: the model-free matrix
      (`plugin/tests/test-resolve-args.sh`) is green, including the `cmp`
      identity case for this wrapper's vendored resolver.
- [ ] Skill-local script tests (if the skill ships any) are green.
- [ ] Prose reviewed against superpowers:writing-skills (authority on
      authorship; certification does not restate it).
- [ ] GREEN transcript shows no shell command a mattstack MCP tool covers.

`rt skills check --strict` is the merge gate in CI and `rt skills sync`
refuses on hits for the mattstack pack; a team pack sees hits as advisory
on sync until its manifest sets `"strictLint": true`. A deliberate don't
that quotes a shell form carries `<!-- mcp-lint: allow -->`.

## Ledger

| date | skill | classification | certify | desc-test | notes |
|------|-------|----------------|---------|-----------|-------|
| 2026-08-09 | mattstack:shepherdr | needs-extraction | pass | 4/4 scenarios | de-personalized 8 literal "Matt" hits to "the user"/"the operator" (SKILL.md:139, scripts/spawn-agent.sh:163, references/job-template.md:25,33,46,60, references/cloud-lane.md:94,99,107); normalized 3 em dashes in cloud-lane.md:32,79,104 (2 beyond the plan's called-out line, found on re-grep) |
| 2026-08-10 | mattstack:model-tiering | pure | pass | 4/4 | no changes |
| 2026-08-10 | mattstack:getting-current-time | pure | pass | 4/4 | no changes |
| 2026-08-10 | parameterized-skills | pure | pass | 4/4 | RED found purity-personal (personal path in convention.md:175 example JSON, missed by D1 first pass); fixed to a generic placeholder path |
| 2026-08-10 | mattstack:work | pure | pass | 5/5 (work scenario added in T10) | certified during T10; ledger row recorded in T21; re-run of certify.sh exit 0 at ledger time |
| 2026-08-10 | mattstack:stage-provision | pure | pass | n/a (hidden, excluded from roster) | certified during T11; re-run exit 0 at ledger time |
| 2026-08-10 | mattstack:stage-plan | pure | pass | n/a (hidden) | certified during T11; re-run exit 0 at ledger time |
| 2026-08-10 | mattstack:stage-gates | pure | pass | n/a (hidden) | certified during T12; re-run exit 0 at ledger time |
| 2026-08-10 | mattstack:stage-evidence | pure | pass | n/a (hidden) | certified during T12; re-run exit 0 at ledger time |
| 2026-08-10 | mattstack:stage-implement | pure | pass | n/a (hidden) | certified during T13; slotless (no vendored resolver); re-run exit 0 at ledger time |
| 2026-08-10 | mattstack:stage-self-review | pure | pass | n/a (hidden) | certified during T13; re-run exit 0 at ledger time |
| 2026-08-10 | mattstack:stage-ship | pure | pass | n/a (hidden) | certified during T14; re-run exit 0 at ledger time |
| 2026-08-10 | mattstack:stage-watch-ci | pure | pass | n/a (hidden) | certified during T14; re-run exit 0 at ledger time |
| 2026-08-18 | mattstack:review-core | pure | pass | 5/5 | harvest integration (2026-08-12) certified; ledger row + desc-test scenario added at SKILLS-36 closeout; certify re-run exit 0 |
| 2026-08-18 | mattstack:review-dispatch | pure | pass | no scenario (deliberate) | selection niche is shadowed by its callers (self-review / review-core absorb every task phrasing, verified 3 wordings x 3-5 reps); reached via REQUIRED SUB-SKILL, so no scenario encodes that reality; certify re-run exit 0 |
| 2026-08-18 | mattstack:self-review | pure | pass | 5/5 | harvest integration certified; scenario added at closeout; certify re-run exit 0 |
| 2026-08-18 | mattstack:receive-review | pure | pass | 5/5 | harvest integration certified; scenario added at closeout; certify re-run exit 0 |
| 2026-08-18 | mattstack:review-posting | pure | pass | 5/5 | harvest integration certified; scenario added at closeout; F4 note: Gate 1/2 HARD-GATE tags to be restored on the next writing-skills-gated edit |
| 2026-08-18 | mattstack:ci-forge-gitlab | pure | pass | n/a (hidden, slot-reached) | harvest integration certified incl. F3 fix (jobs --scope native-status filter); certify re-run exit 0 |
| 2026-08-18 | mattstack:stage-watch-ci | pure | pass | n/a (hidden) | rewritten by the ci-engine harvest (forge split) incl. F1 fix (ref forwarded to all three triage call sites); certify re-run exit 0 |
| 2026-09-04 | mattstack:gate-protocol | pure | pass | n/a (hidden, include-reached) | new shared pane-protocol part (gate facility, engine 0.14.0); certify 10/10 at authoring and at review |
| 2026-09-04 | mattstack:review-posting | pure | pass | 5/5 (scenarios unchanged) | execution-only rewrite: caller-decided {levels, disposition}, never asks; the 2026-08-18 F4 note (restore Gate 1/2 tags) closes as moot, the two-gate protocol is retired; certify 10/10 |
| 2026-09-04 | mattstack:review | pure | pass | 5/5 (scenarios unchanged) | Deliver step reworked to caller-owned decisions with the one combined fallback question; decision record moves to scope post with --decided-by surface names; certify 10/10 |
| 2026-09-04 | mattstack:gate-protocol | pure | pass | n/a (hidden, include-reached) | Runs integration recipe added (facility publish, attendance rule, bounded wait, decided-by surfaces); certify 10/10 at authoring and review |
| 2026-09-04 | mattstack:receive-review | pure | pass | 5/5 (scenarios unchanged) | caller-owned two-gate protocol (respond-plan, respond-post); verdicts/fixes retired; adjudication and execution halves unchanged; certify 9/9 |
| 2026-09-04 | mattstack:shepherdr | pure | pass | 5/5 (scenarios unchanged) | run-backed herd questions ride the gate registry (one subscription, worktree-key list-and-match, answers --by shepherd); herd scripts untouched; all 3 script suites green |
| 2026-09-04 | (12 verb dirs: stage-plan, stage-provision, stage-evidence, stage-ship, stage-watch-ci, ship, watch-ci, work, self-review, sync-open-mrs, rebase-worktree, checkout) | pure | pass | n/a (site adoption) | v4 gate-site adoption: publish per gate-protocol Runs integration, questions verbatim, decided-by from the answer; certify 9-10/10 each at task review and release |
| 2026-09-05 | mattstack:gate-protocol | pure | pass | n/a (hidden, include-reached) | W4 T16: origin/context stamping, presentation-keyed nudge (form vs wait, form-first on herdr panes), idle background wait with the waiting-gate marker set/clear, doorbell priming for form-first pipeline panes; bounded 90s poll loop retired; certify 10/10 |
| 2026-09-05 | checkout | pure | pass | n/a (site adoption) | W4 T18: spawned no-run context guard -- a spawned pane with no armed run errors out visibly instead of presenting an unwatched form (gate clarify); certify 10/10 |
| 2026-09-05 | rebase-worktree | pure | pass | n/a (site adoption) | W4 T18: spawned no-run context guard -- spawned panes with no armed run error out visibly instead of presenting unwatched forms (gates conflict:rebase-worktree and push); certify 10/10 |
| 2026-09-05 | mattstack:gate-protocol | pure | pass | n/a (hidden, include-reached) | W4 final-review fix wave: Unattended step rewritten to marker+background-wait+end-turn (no more foreground `gate wait` block); attended non-herdr panes carved out of the idle wait at Presentation, step 3, step 4, and the quick-reference table so wait belongs only to unattended panes and over-cap herdr panes; the context recipe now builds and measures `$CONTEXT` and structurally omits `--context` past 8192 bytes instead of shipping `--context ""`; strict-membership section reworded to value-only for labeled options; certify 10/10 |
| 2026-09-05 | rebase-worktree | pure | pass | n/a (site adoption) | W4 final-review fix wave: dirty-tree gate gained the spawned no-run guard its conflict/push siblings already had, now routes through gate-protocol's Runs integration, and `--decided-by` is fixed from the verb name `rebase-worktree` to the answer's surface; certify 10/10 |
| 2026-09-05 | checkout | pure | pass | n/a (site adoption) | W4 final-review fix wave: re-certified unchanged after the gate-protocol prose fixes; certify 10/10 |
| 2026-09-05 | mattstack:shepherdr | pure | pass | 5/5 (scenarios unchanged) | W4 final-review fix wave: gate relay step 2/3 gained the labeled-option rule (render `label`, submit `value`) the question-relay path already had; compiled fill recompiled via `rt skills compile`; `rt skills check` reports current |
| 2026-09-08 | mattstack:receive-review | pure | pass | n/a (description unchanged) | respond-plan reshaped to one single-select per thread (BOARD-23): 3-option questions stay under the form cap for any thread count; thread id joined from the option value, never the `thread-<n>` id; `skip` sentinel on code-changes; contradictory-selection re-ask retired; Iterate/Hold placed on a pane-only question; certify 10/10 |
| 2026-09-08 | rebase-worktree | pure | pass | n/a (description unchanged) | SKILLS-45: `rt sync --json` fast path ahead of the manual guards, exit-code table (0 synced+pushed, 3 conflict gate, 4 stack-refusal refuses, 4 stack-check-unavailable opens the manual guards), push gate scoped to the manual path; 6 fixture scenarios RED->GREEN in `docs/superpowers/plans/red-fastpath-rebase-worktree.md`; certify pass |
| 2026-09-08 | sync-open-mrs | pure | pass | n/a (description unchanged) | SKILLS-45: step 3 states that rt-sync-synced branches come back pushed; batch push multi-select names the still-unpushed branches; sweep fixture GREEN; certify pass |
| 2026-09-22 | mattstack:receive-review | pure | pass | n/a (description unchanged) | gate-ctx@1 emission (respond-gate program): steps 4/6 build fitted structured opens on both paths via scripts/gate-ctx.sh (validate, shared-8KB fit, trim order sparing reply text, whole-gate prose fallback reporting fits, pane flatten); one-name reviewer rule; verdict vocabulary carried verbatim; 58/58 script tests; micro-tests RED->GREEN (log beside the plan doc); certify re-run exit 0 at ledger time |
| 2026-09-22 | mattstack:gate-protocol | pure | pass | n/a (hidden, include-reached) | gate-ctx@1 section: the structured-context contract (four shapes on one discriminant tag, parse rules, the shared 8192-byte budget); certify re-run exit 0 at ledger time |
| 2026-09-22 | mattstack:receive-review | pure | pass | n/a (description unchanged) | review-shapes-emitter merge: gate-ctx.sh requires the findings@1 shape, floors round at 1, and fixes a replies crash; certify re-run exit 0 |
| 2026-09-22 | mattstack:review | pure | pass | n/a (description unchanged) | review-shapes-emitter merge: review-source.sh added to build the review-post source from the findings file; gate-ctx.sh vendored with a cmp guard; Deliver builds the review-post open from the findings file and hands it back or runs it; 47/47 review-source tests; certify re-run exit 0 |
| 2026-09-22 | mattstack:review-core-body-tail | pure | pass | n/a (description unchanged) | review-shapes-emitter merge: findings file moves to version 2 (full bodies, evidence, re-review fields); micro-tests rerun tool-less; certify re-run exit 0 |
| 2026-09-22 | mattstack:gate-protocol | pure | pass | n/a (hidden, include-reached) | review-shapes-emitter merge: review@1 and findings@1 added to the structured context table; RED failure excerpts quoted in the evidence; micro-tests rerun tool-less; certify re-run exit 0 |
| 2026-09-22 | mattstack:receive-review | pure | pass | n/a (description unchanged) | respond-post per thread: one multi question per offered thread with independent `post:` / `resolve:` options (resolve recommended for fixes only) replaces `replies` plus the blanket `disposition`; per-thread act; decision record `{threads: {<id>: {post, resolve}}}`; gate-ctx.sh validates `reply@1` in place of `replies@1` (multi required, one question per thread, non-blank strings); 126/126 script tests; RED 0/5 -> GREEN 5/5 on build and act, resume 5/5 both sides after an opus review fix wave (docs/superpowers/tests/2026-09-22-respond-post-per-thread); certify re-run exit 0 |
| 2026-09-22 | mattstack:review | pure | pass | n/a (description unchanged) | respond-post per thread: vendored gate-ctx.sh re-synced byte-identical with receive-review's; review-source tests pass; certify re-run exit 0 |
| 2026-09-22 | mattstack:gate-protocol | pure | pass | n/a (hidden, include-reached) | respond-post per thread: `reply@1` added to the structured context table, `replies@1` kept as the retired shape renderers still read; covered by the receive-review scenarios' compiled system file; certify re-run exit 0 |
| 2026-09-22 | mattstack:spawned-no-run-guard | pure | pass | n/a (hidden, include-reached) | include body drops its `# Spawned, no-run guard` H1 so the four mid-section seams (checkout clarify; rebase-worktree clean-tree, conflict, push) read inline instead of opening a top-level section that swallowed host rules; no host edits; heading-chain retrieval RED 1/5 -> GREEN 5/5 on compiled rebase-worktree (docs/superpowers/tests/2026-09-22-guard-include-heading); certify re-run exit 0 |
| 2026-09-22 | mattstack:receive-review | pure | pass | n/a (description unchanged) | respond-post edited text: step 6's act posts an answer object's `text` in place of the `reply@1` draft for a `post:` thread (`text` without `post:` posts nothing); the record entry adds `text` only for such a thread, `resolve` following the answer; the pane's answer never carries `text` (a typed edit is a note); a note rides its thread's record entry as `note`; the `reply@1` fill is the drafted reply. Strict scoring (exact posted text, record, close): post-act-edited RED 0/5 -> first commit 4/5 (one skipped close, also 1/10 on the pre-edit control) -> committed 5/5; the first commit's act 10/10 and resume 8/10 (one skipped close, one `--` rewrite, 1/15 on the pre-edit control) followed two record-wording iterations after reps over-recorded `text`; fix round 1: post-pane-typed RED 0/5 -> 5/5, post-only 10/10 (a placeholder `resolve` drifted up to 4/15; the concrete example plus a condition holds); fix round 2 (note per thread): pane RED 3/5 -> 5/5; committed engine 5/5 each on build, none, act, resume, post-act-edited, post-only and pane (docs/superpowers/tests/2026-09-22-respond-post-edited-text); certify re-run exit 0 |
| 2026-09-22 | mattstack:gate-protocol | pure | pass | n/a (hidden, include-reached) | respond-post edited text: "Answers are option values" adds the `text` field beside `note` on the answer object, replacing only what the verb's act step names, and says the in-pane form never sends `text` (a replacement for offered text typed in its free-text field rides as a note; scoped so an open-ended question's typed answer, as in stage-evidence's intake, is untouched); with receive-review's pane clause, took post-pane-typed from 1/5 to 5/5, and the rescoped sentence holds 5/5; covered by the receive-review scenarios' compiled system file; certify re-run exit 0 |
| 2026-09-23 | mattstack:gate-protocol | pure | pass | n/a (hidden, include-reached) | note scope: "Answers are option values" drops "The note never replaces anything; `text` replaces only what the verb's own act step names" for "What a note or `text` changes is each verb's own act step's call; the protocol swaps neither in for offered text by itself", so the shared sentence no longer overrides a verb whose own steps take the note as its text; stage-plan-rename (pane composes the answer; strict: renamed line in plan and record, note never `text`, stage finishes; answer shape unscored in every arm) RED 3/10 (6/10 kept the old line) -> control 0.17.23 5/5 (3/5 on a literal reading, two id-less list answers) -> committed 9/10 (10/10 renamed; the miss sent `text` from the pane, as RED did 1/10); a shorter candidate renamed 5/5 but sent `text` 4/5; stage-provision slug probe RED 0/10, control 5/10, GREEN 4/10 (back to its control level but still losing 6/10, which is stage-provision's own wording); receive-review post-pane-typed and post-act-edited 5/5 strict (docs/superpowers/tests/2026-09-23-gate-protocol-note-scope); certify re-run exit 0 |
| 2026-09-22 | mattstack:writing-style-floor | pure | pass | n/a (hidden, include-reached) | new shared floor for the writing-style presets; REFACTORed twice from fixture runs (evidence gets an output example; suggested code is always fenced); certify exit 0 |
| 2026-09-22 | mattstack:writing-style-sparse | pure | pass | 11/11 existing (8-skill roster); no scenario, lookup-gated | new preset: RED baseline, reference, and GREEN reps in docs/superpowers/tests/2026-09-22-writing-style-presets/results.md; three fix rounds (process-ask recipe, two floor REFACTORs, examples no longer mirror the fixtures); auto-trigger check 10/10 NONE; certify exit 0 on engine and compiled |
| 2026-09-22 | mattstack:writing-style-conversational | pure | pass | 11/11 existing; no scenario, lookup-gated | new preset and the resolver's fallback; GREEN 3 reps ok floor, every plan check holds; certify exit 0 on engine and compiled |
| 2026-09-22 | mattstack:writing-style-structured | pure | pass | 11/11 existing; no scenario, lookup-gated | new preset; GREEN 3 reps ok floor, every plan check holds; certify exit 0 on engine and compiled |
| 2026-09-23 | mattstack:writing-style-floor | pure | pass | n/a (hidden, include-reached) | coderabbit review fix: evidence-as-output scoped to review comments and replies; a PR description's verification section keeps reporting test counts, not logs, per each preset's own rule; certify re-run exit 0 |
| 2026-09-23 | mattstack:writing-style-sparse | pure | pass | 12/12 existing; no scenario, lookup-gated | coderabbit review fix: pr-description ticket prefix made conditional on the repo using one; the credit example reordered to lead with the change, not praise; certify re-run exit 0 on engine and compiled |
| 2026-09-23 | mattstack:writing-style-conversational | pure | pass | 12/12 existing; no scenario, lookup-gated | coderabbit review fix: pr-description ticket prefix made conditional on the repo using one; the credit example reordered to lead with the change, not praise; certify re-run exit 0 on engine and compiled |
| 2026-09-23 | mattstack:writing-style-structured | pure | pass | 12/12 existing; no scenario, lookup-gated | coderabbit review fix: pr-description ticket prefix made conditional on the repo using one; certify re-run exit 0 on engine and compiled |
| 2026-09-23 | mattstack:writing-style-sparse | pure | pass | 12/12 existing; no scenario, lookup-gated | coderabbit second-pass fix: the credit example still opened on credit ahead of the concern; reordered so the worry comes first and the credit folds in after; certify re-run exit 0 on engine and compiled |
| 2026-09-23 | mattstack:writing-style-conversational | pure | pass | 12/12 existing; no scenario, lookup-gated | coderabbit second-pass fix: the credit example still opened on credit ahead of the concern; reordered so the worry comes first and the credit folds in after; certify re-run exit 0 on engine and compiled |
| 2026-09-23 | mattstack:stage-provision | pure | pass | n/a (hidden) | typed slug: the `slug` question's bullet now says a slug typed in the pane form rides as the note on that answer, with the value left as the option the form offered, and that note is the slug to use; stage-provision-slug (strict: typed slug in the record and `--title`, the pane's answer `{"value":"suggested","note":<slug>}`, `--by pane`) baseline 6/10 on 0.18.0 -> slug candidate 13/15 strict with the old gate-protocol (15/15 slug used; the 2 misses are list-shaped answers) -> 10/10 with the shipped gate-protocol, 0 list-shaped, 0 `text` (docs/superpowers/tests/2026-09-23-followups-slug-pane-decider); certify re-run exit 0 |
| 2026-09-23 | mattstack:gate-protocol | pure | pass | n/a (hidden, include-reached) | pane answer shape: the form branch submits the chosen values "in one object keyed by question id" and shows `{"<question id>": "<value>" \| ["<value>", ...] \| {"value": ..., "note": "..."}}` in place of `'<json>'`; stage-plan-rename's answers went from three shapes (5 plain, 4 `{"value": ...}`-wrapped, 1 list) to one shape in 10/10, and `next` wrapping fell from 4/10 to 0/10 there and from 8/20 to 0/20 on post-pane-typed; list-shaped answers went from 1/30 to 0/30 across the two target scenarios, with pane `text` 0 in both arms; scenario criteria unchanged: stage-plan-rename 10/10 -> 10/10, post-pane-typed 19/20 -> 19/20, post-act-edited 10/10 -> 9/10 (each miss in either arm is a top-level `glab mr note` improvised by an unfilled compile); certify re-run exit 0 |
| 2026-09-23 | mattstack:review | pure | pass | n/a (description unchanged) | clarify record: `--decided-by review` becomes `--decided-by <the answer's by>`, as self-review and checkout already write it; a tree-wide grep found no other gate record naming a verb (stage-plan's `execution-strategy@1` record keeps `--decided-by stage-plan` on purpose: a v2 slot decision, not a gate); mechanical, no reps; certify re-run exit 0 |
| 2026-09-23 | mattstack:stage-provision | pure | pass | n/a (hidden) | fix round: the slug bullet now says a slug they type arrives as the answer's note (or its `text`, from a surface that edits offered text), with the value left as the option they had picked, and that typed slug is the one to use; stage-provision-slug 5/5 strict, 0 pane `text` (docs/superpowers/tests/2026-09-23-followups-slug-pane-decider); certify re-run exit 0 |
| 2026-09-23 | mattstack:gate-protocol | pure | pass | n/a (hidden, include-reached) | fix round: `--decided-by`'s surface rule and WINNER rule are scoped to `gate@1` records; the form branch says a question with no options takes what the human typed as its value (the rt-client validator checks membership only when options exist), and "Answers are option values" scopes its match rule to a question with options; the Publish block shows `--questions` as `'[{"id": ..., "label": ..., "options": [...]}, ...]'` and Runs integration as `'<questions json array>'`; evidence-intake-open RED 1/3 clean (2/3 sent `text` from the pane) -> 3/3 -> 3/3 after the scope fix; review-clarify daemon-valid questions array 3/5 -> 5/5; stage-plan-rename 5/5 and post-pane-typed 5/5 after each change; certify re-run exit 0 |
| 2026-09-23 | mattstack:review | pure | pass | n/a (description unchanged) | fix round: the clarify gate runs gate-protocol's Runs integration with kind `clarify` (it had asked through the structured-question tool, so no registry answer stood behind `<the answer's by>`), with questions `target` and `next`: **Proceed** (recommended) / **Hold**; review-clarify RED 1/5 opened a registry gate -> 5/5 routed (3/5 strict) -> 5/5 strict, with a daemon-valid array and Hold in `next`; certify re-run exit 0 |
| 2026-09-23 | checkout | pure | pass | n/a (description unchanged) | fix round: the clarify gate's Hold moves from the candidate question to its own `next` question (`branch`, one option per candidate or their text, then `next`: **Proceed** (recommended) / **Hold**), matching review's tested shape; no checkout-specific reps; certify re-run exit 0 |
| 2026-09-23 | mattstack:self-review | pure | pass | n/a (description unchanged) | clarify gate: Hold moves from the candidate-sources question to its own `next` question (`source`, the task as stated, a linked doc, or their text, then `next`: **Proceed** (recommended) / **Hold**), the question id matching the record's `source` key, as review and checkout now do; self-review-clarify (strict: bare array, Hold in `next`, `--by pane`, `--decided-by pane`) RED 0/3, plus 0/3 again before the edit applied -> GREEN 3/3 (docs/superpowers/tests/2026-09-23-followups-slug-pane-decider); certify re-run exit 0 |
| 2026-09-23 | mattstack:review | pure | pass | n/a (description unchanged) | clarify Hold: the gate gains a Hold line in stage-provision's shape, with the record spelled out (`rt runs decision record --contract gate@1 --scope hold:<stage>:<attempt> --selection '{"reason":"<their words>"}' --decided-by <the answer's by>`, then `rt runs field set hold`, end the turn); review-clarify-hold (strict: hold decision record, `hold` field, stop) RED 0/3 -> stage-provision's shorthand 0/3 (3/3 stopped, but all read the record as a gate-field write) -> committed 3/3, each skipping the clarify target record; certify re-run exit 0 |
| 2026-09-23 | checkout | pure | pass | n/a (description unchanged) | clarify Hold: the same Hold line with `<run.current_stage>`; covered by review's probe, no checkout-specific reps; certify re-run exit 0 |
| 2026-09-23 | mattstack:self-review | pure | pass | n/a (description unchanged) | clarify Hold: the same Hold line with `<stage>`; covered by review's probe, no self-review-specific reps; certify re-run exit 0 |
| 2026-09-23 | mattstack:receive-review | pure | pass | n/a (description unchanged) | respond-post fixed only: step 4 reads a `reply:` answer's `text` as the reply to post, records it in a sibling `texts` map (`threads` values stay bare verbs) and writes it over the draft in that thread's report row; the pane's gate 1 answer never carries `text`; step 6 offers only threads a fix finalized in step 5, reply-only threads post their gate 1 reply unresolved (right away with no respond-post gate or record when nothing is offered, else after gate 2 with its picks), the respond-post record covers offered threads only, and a gate opened before this rule that still offers a reply-only thread decides it (an empty array included) with no reply posted twice. Strict: plan-replies-only RED 0/5 -> 5/5, plan-fix-and-reply RED 0/5 -> 5/5, plan-all-skip 5/5 guard; legacy-post-act 4/5 (empty-array loophole) -> 5/5; regressions on the committed engine post-build, post-act, post-resume, post-none, post-pane-typed, post-only 5/5 each, post-act-edited 9/10 (one self-corrected token slip); five older scenarios updated for the retired premise (docs/superpowers/tests/2026-09-23-respond-post-fixed-only); certify re-run exit 0 |
| 2026-09-23 | mattstack:receive-review | pure | pass | n/a (description unchanged) | respond-post fixed only, fix round 1 (review): step 4 rewrites every report row as `- <threadId> · <file>:<line> · <verdict>, recommended <action> · gate-1: <reply\|fix\|skip\|override> · reply: "<text>"` (step 5 adds ` · sha: <short sha>`), and all posting, a resume included, reads `gate-1`, never the recommendation; a `reply:` answer with `text` posts it, note or not; a `reply:` with no `text` on a non-verbatim card, or carrying a note, is `override`: redrafted and offered at gate 2 (`verb` `reply`, no `sha`, resolve not recommended); gate 2 offers overrides and finalized fixes; `gate-1: reply` rows post once gate 2 proceeds, never on hold or iterate; the caller-owned path hands back which replies posted. Strict: plan-override-reply, plan-override-reply-text and plan-override-note RED 0/5, the first commit's plan reps 0/5 on the rows criterion, resume-skipped-reply and row-shape post-resume 5/5 guards; the committed engine 5/5 on all twelve (override, override-text, override-note, resume-skipped-reply, post-resume, replies-only, fix-and-reply, all-skip, post-act, post-act-edited, post-build, legacy); post-resume rewritten to the row shape, its text changes disclosed (docs/superpowers/tests/2026-09-23-respond-post-fixed-only); certify re-run exit 0 |
| 2026-09-23 | mattstack:receive-review | pure | pass | n/a (description unchanged) | respond-post fixed only, fix round 2: a dropped gate 1 question context is a third override trigger, whoever answered; the respond-plan record lists `"overrides":["<threadId>"]` beside `texts`, omitted when empty, and a snapshot-only `## Run` Resume reads it; "an open built before this rule" replaces "an open in the retired shape"; the quick-reference `reply:` row reads one way. Strict: plan-override-dropped RED 0/5 (failed only the new `overrides` record) -> 5/5; plan-override-note, plan-replies-only, resume-skipped-reply, post-none, post-pane-typed and post-act-edited-postonly 5/5 each; certify re-run exit 0 |
| 2026-09-23 | mattstack:receive-review | pure | pass | n/a (description unchanged) | respond-post fixed only, fix round 3: a whole-open signal (a `"fits": false` open, or `contextOmitted`) counts every thread's context as dropped, as the board:respond wrapper does, and the rationale reads "so its draft may never have been shown"; after `contextOmitted` the verb never shortens a reply and never re-asks, overriding the stderr's "shorten and re-ask", and takes the answers as given, every no-`text` `reply:` an override (plus a red flag); the snapshot sentence reads "the reply overrides, offered at gate 2 beside any finalized fix". Strict: plan-override-dropped rewritten with the real stderr line and an over-budget source; RED on fix round 2's engine 5/5 (the real stderr already names every question context), GREEN 5/5 (both threads `override`, `"overrides":["T1","T2"]`, both offered at gate 2, nothing posts, no shortening, no re-ask); plan-dropped-at-ask (no answer yet) 5/5 on both engines, none re-asks; resume-snapshot-overrides 5/5 on both; plan-override-note and plan-replies-only 5/5; certify re-run exit 0 |
| 2026-09-23 | mattstack:receive-review | pure | pass | n/a (description unchanged) | respond-post fixed only, fix round 4 (final review): the pane that opens respond-plan over budget (`"fits": false`, or `contextOmitted`) writes one line, `gate-1-context: dropped`, into step 3's saved report right after the ask and before waiting on an answer, and any resume (a caller-handed `{plan}` in a fresh pane included) that finds the line counts every thread's context as dropped, whoever opened the gate (the board:respond wrapper writes and reads the same line); a snapshot-only `## Run` Resume counts a `reply` thread with no `texts` entry as an override (`gate-1: override`), redrafted and offered at gate 2 beside the other overrides, never posted from gate 1; the dropped line goes into the saved report when there is one; two red-flag rows. Strict: plan-dropped-at-ask on its new write criterion RED 0/5 -> 5/5; resume-plan-dropped 5/5 on both engines (the reader already read the line), its no-line control RED 0/5 (both unseen drafts posted); resume-snapshot-plain-only (pressure: "just post", a release cut, a recoverable-looking draft) RED 0/5 (every rep posted a rebuilt reply) -> 5/5, its first draft (the same pressure, without the sentence about the lost draft's substance) 1/5 with four different handlings; resume-snapshot-plain-reply 5/5 on both; after the override wording, plain-only and plain-reply re-run 5/5 each, every rep naming the thread an override; regressions resume-snapshot-overrides, resume-skipped-reply and plan-override-dropped 5/5, post-resume 9/10 (one positional-join slip in the reasoning, record right; base control 5/5) (docs/superpowers/tests/2026-09-23-respond-post-fixed-only); certify re-run exit 0 |
| 2026-09-23 | mattstack:receive-review | pure | pass | n/a (description unchanged) | respond-post fixed only, fix round 5 (CodeRabbit on skills#10): the respond-plan record gains `notes` (one entry per `gate-1: override` thread whose answer carried a note, omitted when empty), which a snapshot-only Resume folds into the override's redraft; the no-offer path splits: with no caller-handed `post` the `gate-1: reply` rows post at once as before, and with one (in `{plan, post}` or alone) that `post` decides first and its selection is recorded, a retired-shape `post` unchanged; a retired-shape `post` decides every `gate-1: reply` row (listed posts once, omitted posts nothing, an empty list included). Strict: plan-override-note on its `notes` criterion RED 0/5 -> 9/10 (one rep skipped the gate answer); resume-snapshot-note-override 5/5 on both (the reader already folded the note); plan-post-retired-empty RED 0/5 (every rep posted T1) -> 5/5; resume-post-retired-empty 5/5 guard, 4/5 on the first wording (a translated record) -> 5/5; regressions plan-replies-only, post-none, resume-skipped-reply, post-resume and resume-snapshot-overrides 5/5 each ; stand-in review text round: a retired-shape `post` decides a `gate-1: reply` row fully (a listed row resolves only on `resolve-addressed`), stated at step 6 and in the quick reference, whose `reply:` row also names the handed `post`; plan-post-retired-listed 5/5 before and after (guard), plan-post-retired-empty and plan-replies-only 5/5 (docs/superpowers/tests/2026-09-23-respond-post-fixed-only); certify re-run exit 0 |
| 2026-09-25 | mattstack:review | pure | pass | n/a (description unchanged) | rt MR tools: Posting mechanics on GitLab names `mr_comment` for the ONE summary and `mr_approve` for the Approve disposition beside `mr_comment_inline` / `mr_reply_thread`; the summary posts resolvable when its issue list carries an unanchored selected finding, `resolvable: false` otherwise; `mr_comment` returns `mrUrl` for the close. Neutral deferred-tool catalog: RED left `resolvable` unguided (one rep `false` beside an unanchored finding) and 3/5 ran `glab mr view` for the close URL; GREEN 2/2 (resolvable true with F2, false with anchored-only, close from `mrUrl`, no `glab`) (docs/superpowers/tests/2026-09-25-mcp-mr-tools); certify 11/11 |
| 2026-09-25 | mattstack:review-posting | pure | pass | n/a (description unchanged) | rt MR tools: the close HARD-GATE, its red-flag row and its quick-reference row read the URL "from the forge (the `mrUrl` a posting tool returned, or the forge CLI)"; RED 3/5 ran a `glab mr view` read only to satisfy the old "forge CLI" wording, GREEN 2/2 close from `mrUrl` (docs/superpowers/tests/2026-09-25-mcp-mr-tools); certify 11/11 |
| 2026-09-25 | mattstack:receive-review | pure | pass | n/a (description unchanged) | rt MR tools: step 6 names `mr_reply_thread` for a GitLab reply and `mr_resolve_thread` for a resolve (after its reply when both are picked), the forge CLI and adapter for other forges; RED 5/5 already used the tools, GREEN 1/1 no regression (docs/superpowers/tests/2026-09-25-mcp-mr-tools); certify 11/11 |
| 2026-09-25 | mattstack:ship | pure | pass | n/a (description unchanged) | rt MR tools: the generic path reads the forge from origin, a GitLab host meaning `mr_create` (default branch from `symbolic-ref`, `draft: false` only when the gate said ready) and `mr_ready`, GitHub `gh`; mark-ready's Yes names `mr_ready`. Neutral catalog: RED planned `glab mr create --fill --draft` and `glab mr update --ready` (2/2), GREEN 1/1 `mr_create` then `mr_ready` (docs/superpowers/tests/2026-09-25-mcp-mr-tools); certify 11/11 |
| 2026-09-25 | mattstack:stage-ship | pure | pass | n/a (hidden) | rt MR tools: the forge-host rule reads "the forge" from origin, a GitLab host meaning `mr_create` / `mr_ready`, the GitHub half unchanged; the generic fallback opens the MR "the way the forge-host rule names"; same rule text as ship's covered scenario; certify 11/11 |
| 2026-09-25 | mattstack:watch-ci | pure | pass | n/a (description unchanged) | rt MR tools: mark-ready's Yes names `mr_ready` for GitLab; the neither-bound triage retries an INFRA job with `mr_retry` (`jobId`); the forge-bound retry keeps the report's command, already allow-listed as `Bash(*/scripts/ci-forge.sh:*)`. Neutral catalog: RED `glab mr update 42 --ready` and, unbound, `glab ci retry 812`; GREEN 2/2 `mr_ready` and `mr_retry` (docs/superpowers/tests/2026-09-25-mcp-mr-tools); certify 11/11 |
| 2026-09-25 | mattstack:stage-watch-ci | pure | pass | n/a (hidden) | rt MR tools: mark-ready's Yes names `mr_ready`; the neither-bound triage retries with `mr_retry`; the forge-bound retry and the `ci` gate's Retry keep the report's allow-listed command. RED `glab mr update 42 --ready` (3/3), GREEN 1/1 `mr_ready` (docs/superpowers/tests/2026-09-25-mcp-mr-tools); certify 11/11 |
| 2026-09-26 | mattstack:review | pure | pass | n/a (description unchanged) | MCP tools over Bash (skills#21): runDb from `run_start`/`run_list` passed on every `run_*` call, no `export`/`unset RT_RUN_DB`; target from `mr_view` (`mr_for_branch` for a branch), diff by git reads of a named MR ref; review-post gate through `gate_ask`, pane answer through `gate_answer`. RED `rt runs --repo`, `export RT_RUN_DB`, `glab mr view`/`glab mr diff`, `rt gate ask ... jq`, `unset RT_RUN_DB`; GREEN `run_list`, `run_stage`, `mr_view`, `gate_ask`, `gate_answer`, `run_decision`, `run_status`, no rt/glab Bash (commit bodies); certify re-run exit 0 |
| 2026-09-26 | mattstack:self-review | pure | pass | n/a (description unchanged) | MCP tools over Bash (skills#21): `run_start {flags, skillDir}` replaces `PACK_DIRS` + `rt runs run-start`; inherited runs read with `run_snapshot`. RED `PACK_DIRS=...`, `rt runs run-start`, `export RT_RUN_DB`, `rt runs decision record`; GREEN `run_list`, `run_start`, `run_stage`, `run_field_set`, `run_decision`, `run_status`, no Bash; certify re-run exit 0 |
| 2026-09-26 | mattstack:receive-review | pure | pass | n/a (description unchanged) | MCP tools over Bash (skills#21): threads through `mr_threads {refresh: true}`, respond gates through `gate_ask`/`gate_answer`, push through `git_push`. RED `glab api .../discussions`, `rt gate ask ... jq`, `git push origin <branch>`; GREEN `run_start`, `mr_threads`, `gate_ask`, `git_push {tree}`, `run_decision`, `run_status`, Bash only for two git reads; certify re-run exit 0 |
| 2026-09-26 | mattstack:review-posting | pure | pass | n/a (description unchanged) | MCP tools over Bash (skills#21): the close URL comes from `mr_view`. RED 1/3 `glab mr view`, 2/3 guessed `mr_view` arguments; GREEN 3/3 `mr_view {mrUrl}`; certify re-run exit 0 |
| 2026-09-26 | checkout | pure | pass | n/a (description unchanged) | MCP tools over Bash (skills#22): `mr_view` then `worktree_provision`; the sibling `git worktree add` fallback is gone. RED `glab mr view` x2, `rt worktree provision` (guessed key, failed), `rt worktree list --json`; GREEN `mr_view`, a `git worktree list` read, `worktree_provision {repoName, branch, ticketTitle}`; certify re-run exit 0 |
| 2026-09-26 | rebase-worktree | pure | pass | n/a (description unchanged) | MCP tools over Bash (skills#22): `branch_sync {tree}` replaces `cd <tree> && rt sync --json`; stops on its safety refusals and cache-aware stack guards. RED `rt sync --json`; GREEN git reads then `branch_sync` (status synced, pushed); certify re-run exit 0 |
| 2026-09-26 | sync-open-mrs | pure | pass | n/a (description unchanged) | MCP tools over Bash (skills#22): run family on tools, MRs from `mr_list` (others' branches deselected unless the username is known), trees from `rt_verb worktree list`. RED `rt runs` x7, `PACK_DIRS`, `export`/`unset RT_RUN_DB`, `glab mr list`/`view`, `rt worktree list`; GREEN `run_list`, `run_start`, `run_stage`, `mr_list`, `rt_verb`, `run_field_set`, `run_decision`, `run_status`; certify re-run exit 0 |
| 2026-09-26 | map-open-mrs | pure | pass | n/a (description unchanged) | MCP tools over Bash (skills#22): `mr_list` in place of `glab mr list`, with an author column since the tool has no author filter; covered by the sync-open-mrs scenario; certify re-run exit 0 |
| 2026-09-26 | mattstack:gate-protocol | pure | pass | n/a (hidden, include-reached; description drops `rt gate ask`) | MCP tools over Bash (skills#22): publish through `gate_ask`, answer through `gate_answer` (a lost CAS returns `conflict: true` with the winner's `row`), markers and records through `run_field_set`/`run_decision` on the verb's runDb; `gate_ask` and `gate_answer` appear as planned calls in the review verbs' GREEN listing runs, not executed live (the forge verbs' gates were narrated), and no run exercised the lost-CAS path; certify re-run exit 0 (static checks) |
| 2026-09-26 | mattstack:run-identity | pure | pass | n/a (hidden) | MCP tools over Bash (skills#22): the run is carried as a runDb, not `RT_RUN_DB`; mechanical, covered by the forge verbs' GREEN runs; certify re-run exit 0 |
| 2026-09-26 | mattstack:work | pure | pass | n/a (description unchanged) | MCP tools over Bash (skills#23): `run_start` then runDb in context for every `run_*` call and across compaction; Resume through `run_list`. RED `PACK_DIRS`, `rt runs run-start`, `export RT_RUN_DB`; GREEN fail gate to Hold, then Resume from a new session with `run_stage`/`run_list` executed for real; all eight stages end to end, 53 run tool calls, run closed abandoned; certify re-run exit 0 |
| 2026-09-26 | mattstack:ship | pure | pass | n/a (description unchanged) | MCP tools over Bash (skills#23): run family on tools; push through `git_push {tree, setUpstream}`, with the plain git fallback (allow-marked) only on its unregistered-repo or daemon-down error. RED `git push -u origin <branch>`; GREEN `git_push`, `mr_create`, `run_stage done`, `run_status done`; certify re-run exit 0 |
| 2026-09-26 | mattstack:watch-ci | pure | pass | n/a (description unchanged) | MCP tools over Bash (skills#23): `mr_for_branch`, `mr_pipeline` (~60s cadence, 45 min timeout), `mr_job_trace`, `mr_retry`, `git_push` on Fix and re-push; no-MR GitLab goes to the ci gate. RED `glab mr list --source-branch`, `glab ci status --live`; GREEN `mr_for_branch`, `mr_pipeline`, `mr_job_trace`, `git_push`; certify re-run exit 0 |
| 2026-09-26 | mattstack:stage-provision | pure | pass | n/a (hidden) | MCP tools over Bash (skills#23): `worktree_provision` then `EnterWorktree` by path; allowed-tools line dropped; the gate keys on the tool's real errors. RED `rt worktree provision ... --json`; GREEN `worktree_provision`, `EnterWorktree` in the eight-stage run; certify re-run exit 0 |
| 2026-09-26 | mattstack:stage-ship | pure | pass | n/a (hidden) | MCP tools over Bash (skills#23): `git_push` with the same allow-marked fallback as ship. RED `git push -u origin <branch>`; GREEN `git_push {tree, setUpstream: true}`; certify re-run exit 0 |
| 2026-09-26 | mattstack:stage-watch-ci | pure | pass | n/a (hidden) | MCP tools over Bash (skills#23): `mr_pipeline` polling, `mr_job_trace`, `mr_retry`. RED `glab ci status --live --branch`, `glab ci trace`; GREEN `mr_pipeline`, `mr_job_trace` in the eight-stage run; certify re-run exit 0 |
| 2026-09-26 | mattstack:stage-evidence, stage-plan, stage-implement, stage-gates, stage-self-review | pure | pass | n/a (hidden) | MCP tools over Bash (skills#23): run state through `run_stage`, `run_field_get`/`run_field_set`, `run_decision`, `run_status` on the engine's runDb; RED `rt runs run-status` among the stage dry runs; GREEN the eight-stage run, no `rt runs` on Bash; certify re-run exit 0 each |
| 2026-09-26 | mattstack:shepherdr | pure | pass | 5/5 x2 (18/18 matrix) | MCP tools over Bash (skills#24): herd lifecycle through `herd_*`, `rt_verb` for pane peek and endpoint lookup, `worktree_stop_holders` before `herd_wrap_up` disposes; cloud lane reference deleted; job template names `herd_ask`/`herd_answer`/`herd_milestone`/`herd_report`, `run_start` with `spawnedBy`, `gate_ask`. Ruled Bash, allow-marked in this task: the `--dir` spawns (herd_spawn takes no dir), `rt herd stop --hidden`, and README lines a human types. RED `herd_brief` refused on a stale root, `lsof`/`kill`, worker `rt herd ask/answer/milestone/report`; GREEN every herd tool named above, only the ruled lines on Bash; certify re-run exit 0 on engine and compiled |
| 2026-09-26 | mattstack:execution-strategy | pure | pass | n/a (hidden) | MCP tools over Bash (skills#24): names `herd_brief` in place of `rt herd brief`; inlined into shepherdr, covered by its GREEN; certify re-run exit 0 |
| 2026-09-26 | mattstack:parameterized-skills | pure | pass | n/a (hidden) | MCP tools over Bash (skills#24): convention.md's run and gate contract lines move to `run_*` keyed on runDb (cwd fallback kept), compile and check to `rt_verb`, worktree compiles to bare `rt skills compile --pack-dir`. RED `run_stage`/`run_field_set` with a cwd and no runDb; GREEN every run call carries runDb; certify re-run exit 0 |
| 2026-09-26 | mattstack:editing-skills | pure | pass | 5/5 x4 (18/18 matrix) | MCP tools over Bash (skills#24): routine skills verbs through `rt_verb` with `--pack`; `cd <checkout>` as its own call before bare git; a default-branch push stays a bare `git push` (allow-marked in this task), a feature branch goes through `git_push`. RED `git -C ... push origin main`, `rt_verb` with a cwd and no `--pack`; GREEN `rt_verb ... --pack`, bare default-branch push; certify re-run exit 0 |
| 2026-09-26 | mattstack:extending-a-pack | pure | pass | 5/5 x3 (18/18 matrix) | MCP tools over Bash (skills#24): bind, check, surface set, compile and sync through `rt_verb --pack`; one-time setup verbs stay bare Bash (rt_verb refuses them); fenced blocks lose trailing comments. RED `rt_verb` refused on `skills packs`/`composition`, `git -C`; GREEN five `rt_verb ... --pack demo` calls; certify re-run exit 0 |
| 2026-09-26 | mattstack:creating-a-pack | pure | pass | 5/5 x2 (18/18 matrix) | MCP tools over Bash (skills#24): daemon check through `rt_verb`, `rt skills init --repo` with no `cd &&`, the glab row dropped; the zone checkout's default-branch `git push` stays, allow-marked in this task; this task also drops the stale "`glab` was absent" report example (fallback retired in skills#24), keeping the provisioner example alone: RED 3/3 named the retired `glab` fallback, a candidate swapping in the `git_push` fallback led 2/3 to invent CLI fallbacks for `mr_create`/`mr_pipeline`, the shipped one-example text 3/3 clean. RED `rt daemon status --json`, `which glab`, `cd ... && rt skills init`, `git -C ... push`; GREEN `rt_verb daemon status`, bare init; certify re-run exit 0 |
| 2026-09-26 | mattstack:ci-forge-gitlab | pure | pass | n/a (hidden) | strict mcp lint: the adapter's own `glab` runtime dependency (Requirements, the contract's dependency and exit-3 rows) is script behavior, not an agent call; allow-marked, no wording change; certify exit 0 |
| 2026-09-26 | mattstack:mcp-tools | pure | pass | n/a (hidden) | New reference attachment: hand-written SKILL.md plus `reference.md` generated by `scripts/gen-mcp-tools.ts` from `rt mcp tools --json` (70 tools); `tests/test-mcp-tools-reference.sh` fails when the committed copy drifts from what rt generates. Exercised through extending-a-pack's GREEN (subjects cite it by path); certify exit 0 |
| 2026-09-26 | mattstack:extending-a-pack | pure | pass | n/a (no description edit) | mcp-tools pointer in "Write it": open the reference before a fill names a command; the fill's sentence names the tool. Scenario: a fill that pushes the branch after every commit. RED 3/3 FAIL (`git push`, `git push -u origin <branch>`, plain-words push); GREEN iteration 1 (pointer only) 2/3, one fill still `git push`; refactored to state the sentence's shape (`git_push {tree: <checkout>}`), GREEN 3/3 `git_push`; certify exit 0 |
| 2026-09-26 | mattstack:creating-a-pack | pure | pass | n/a (no description edit) | Same mcp-tools pointer as extending-a-pack, under "Offer the first rules, once"; each rule is an extending-a-pack round, so that skill's GREEN covers it, no separate run; certify exit 0 |
| 2026-09-26 | mattstack:editing-skills | pure | pass | n/a (no description edit) | Craft gate's GREEN step asks whether the fresh agent ran a shell command a tool covers (the quoted forms allow-marked). Scenario: judge a GREEN transcript with a feature-branch `git push`. RED 3/3 called it green or flagged only `git add -A`; GREEN 3/3 "not green", fix names `git_push`; certify exit 0 |
| 2026-09-26 | mattstack:mcp-tools | pure | pass | n/a (hidden) | Review fix: the rule sentence applies to calls that have a tool in the reference; the kept-on-Bash exceptions (`git commit` among them) live in the generated header, and their missing tool is intended. Covered by extending-a-pack's re-run GREEN; reference test and certify exit 0 |
| 2026-09-26 | mattstack:extending-a-pack | pure | pass | n/a (no description edit) | Review fix: pointer qualified ("has a tool there, except the few its header lists as staying on Bash"). GREEN re-run 3/3 `git_push`; new scenario, a fill that commits and pushes, 2/2 kept `git commit` on Bash, pushed with `git_push`, invented no commit tool; certify exit 0 |
| 2026-09-26 | mattstack:creating-a-pack | pure | pass | n/a (no description edit) | Review fix: same qualified pointer as extending-a-pack, covered by its re-run GREEN; certify exit 0 |

(Ledger rows are appended by the orchestrating session as each lane lands -- lanes themselves never edit this file.)
