# Fast Browser verification consent

Treat `browser_run_code_unsafe` as privileged and state-changing. Under the
full profile, Fast Browser's exact plugin policy grants this tool approval for
the delegated browser task; it does not grant any broader permission bypass.
Respect the real-Chrome and authentication boundaries in the browser-driver
agent.
