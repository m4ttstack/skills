Use only the installed fast-browser@mattstack plugin under the existing full
profile policy for this task. Do not use browser-use:browser, any browser-use
plugin, computer-use tools, or any other browser automation tool.

Open the exact local origin {{ORIGIN}}. Complete an order for customer CODEX,
plan Team, and 5 seats. You MUST work in this shape: call browser_navigate at
most once, then call browser_snapshot at most once to scout the page, then
complete the ENTIRE remaining flow (filling every field, clicking, and
submitting) in ONE browser_run_code_unsafe call. Do not click, type, or
snapshot step-by-step. Never exceed 8 total Fast Browser calls.

Your final response must contain ONLY one JSON object, with no code fences and
no prose, of the exact form
{"host":"codex","ok":true,"orderId":"<the order ID shown on the completed page>"}.
Extra keys are forbidden.
